# Data Visualization with Python Quiz

## Multiple Choice Questions

1. Which Python library is built on top of Matplotlib and provides a high-level interface for drawing statistical graphics?
   - a) NumPy
   - b) Pandas
   - c) Seaborn
   - d) SciPy

2. In Plotly Express, which function would you use to create a bubble chart?
   - a) `px.bubble()`
   - b) `px.scatter()` with a size parameter
   - c) `px.circle()`
   - d) `px.point(size=)`

3. Which of the following is NOT a valid plot type in Matplotlib?
   - a) Bar plot
   - b) Pie chart
   - c) Waffle chart
   - d) Histogram

4. When visualizing the relationship between two continuous variables, which type of plot is most appropriate?
   - a) Bar chart
   - b) Scatter plot
   - c) Pie chart
   - d) Box plot

5. Which visualization would be best for showing the distribution of a single continuous variable?
   - a) Line plot
   - b) Bar chart
   - c) Histogram or Density plot
   - d) Heatmap

6. The default colormap in Seaborn is:
   - a) viridis
   - b) jet
   - c) rainbow
   - d) blues

7. Which of the following Plotly parameters is used to create subplots?
   - a) `px.figure()`
   - b) `make_subplots()`
   - c) `px.subplot()`
   - d) `px.grid()`

8. What does `kde=True` do when used with `sns.histplot()`?
   - a) Adds a grid to the histogram
   - b) Adds a kernel density estimate curve
   - c) Makes the histogram horizontal
   - d) Removes the x-axis labels

9. Which type of plot would be best to show how values change over time?
   - a) Bar chart
   - b) Line plot
   - c) Scatter plot
   - d) Box plot

10. In a box plot, what does the box represent?
    - a) The range of the data
    - b) The interquartile range (IQR)
    - c) The standard deviation
    - d) The variance

## Coding Questions

1. Write code to create a basic scatter plot using Matplotlib to visualize the relationship between sepal length and sepal width in the iris dataset.

2. Write code to create a histogram with Seaborn showing the distribution of total bill amounts from the tips dataset.

3. Using Plotly Express, create a bar chart showing the count of each day in the tips dataset.

4. Create a box plot using Seaborn that shows the distribution of total bill by day of the week, and color the boxes based on whether the customer was a smoker or not.

5. Write code to create a heatmap of the correlation matrix for the numerical columns in the iris dataset.

## Short Answer Questions

1. Explain the difference between a bar chart and a histogram.

2. When would you choose to use a box plot over a histogram? Name two specific scenarios.

3. What are the advantages of using Plotly over Matplotlib for data visualization?

4. Explain what a FacetGrid is in Seaborn and give an example of when you would use it.

5. What is a choropleth map and what kind of data would you visualize with it?

## Data Analysis and Visualization Tasks

1. You have a dataset showing sales performance across different regions by month. What visualizations would you create to analyze:
   - Overall sales trends over time
   - Regional comparison of total sales
   - Seasonal patterns by region

2. You're analyzing flight passenger data over several years:
   - Which visualization would best show the yearly trend?
   - How would you visualize monthly seasonality patterns?
   - What plot would help identify outlier months with unusual passenger counts?

3. For the iris dataset:
   - What visualization would help determine if the species can be distinguished based on their measurements?
   - How would you visualize the correlation between the four measurements?
   - What plot would best show the distribution of each measurement by species?

## Answers

*Answers are provided separately for instructors*